<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hello.fclover.mybatis.mapper.GoodsMapper">
        <insert id="goodsInsertText">
            insert into Goods
            (seller_id, cate_no, goods_name, goods_content, goods_price, goods_writer, seller_company, goods_create_at,
              goods_count, goods_date, goods_pageCount, goods_bookSize, writer_content)
            values(#{sellerId}, #{cateNo}, #{goodsName}, #{goodsContent}, #{goodsPrice}, #{goodsWriter}, #{sellerCompany}, #{goodsCreateAt},
                   #{goodsCount}, current_date , #{goodsPageCount}, #{goodsBookSize}, #{writerContent})
        </insert>
        <select id="goodsNoselect">
            select goods_no
            from goods
            where seller_id = #{sellerId} and goods_name=#{goodsName}
        </select>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hello.fclover.mybatis.mapper.MemberMapper">
    <insert id="insert">
        INSERT INTO member (member_id, password, name, email, phone, auth) VALUES (#{member_id}, #{password}, #{name}, #{email}, #{phone}, #{auth});
    </insert>

    <select id="isId">
        SELECT * FROM member WHERE member_id = #{id}
    </select>

    <update id="memberUpdate">
        UPDATE member SET password = #{password}, name = #{name}, email = #{email}, phone = #{phone} WHERE member_id = #{member_id}
    </update>
</mapper>